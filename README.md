# 3D 象棋對局場

使用 `Three.js + Vite` 製作的 3D 中國象棋 PWA，支援：

- 手機與 PC 共用介面
- 加入主畫面安裝
- AI 難度切換
- 3D 棋盤、旋轉視角、點擊走子

## 啟動

```powershell
& 'C:\Program Files\nodejs\npm.cmd' install
& 'C:\Program Files\nodejs\npm.cmd' run dev
```

## 建置

```powershell
& 'C:\Program Files\nodejs\npm.cmd' run build
```
